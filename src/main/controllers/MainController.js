/**
 * Controller for the MainController prototype.
 */
export default class MainController {
  /**
   * @constructor
   * @param {!angular.Scope} $scope
   */
  constructor($scope) {
    'ngInject';

    this.$scope = $scope;
  }
}
